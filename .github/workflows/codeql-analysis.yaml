name: CodeQL

on:
  push:
  pull_request:
  schedule:
    - cron: 12 10 * * 4

jobs:
  call-codeql:
    uses: SpongePowered/.github/shared-workflows/codeql-analysis.yaml@master
    with:
      runtime_version: 17
    secrets: inherit
